{
  "name": "weather-esp32-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for fetching and serving weather data to ESP32",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
